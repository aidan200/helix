/**
 * T4.2 —— CL-1×CL-8 重启恢复全部会话（真 daemon，E 层）：两会话 + SubAgent
 * 历史 → daemon 重启 → session.list 全量 + 切回含 SubAgent 历史（撤
 * TR-AD-19 恢复重建面边界的行为面证据）。
 *
 * 填充中（T4.2 增量交付：本文件骨架先行落位，剧本随后逐段补齐）。
 */
import { test } from "./harness/daemon-fixture";

test.describe("T4.2 CL-1 重启恢复全部会话（骨架）", () => {
  // 剧本填充中
});
