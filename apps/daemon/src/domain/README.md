# domain/ — 领域层

充血模型（属性 + 行为），framework-free：不 import pi-*、bun/node API、protocol 类型。
后续任务落位：session（Session/Entry/Turn/SessionSnapshot）、agent（AgentLifecycle/SteerQueue）、
tools（ToolCallRecord）、workspace（WorkspaceGroup）、events（DomainEvent）——见 architecture.md §3.2/§3.3。
